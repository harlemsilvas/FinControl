import type { ReactElement } from 'react';
import { NavLink, Outlet } from 'react-router-dom';
import { useAuth } from '../auth/auth-context';

const links=[['Visão geral','/'],['Contas a pagar','/payables'],['Fornecedores','/suppliers'],['Categorias','/financial-categories'],['Centros de custo','/cost-centers'],['Tipos de documento','/document-types'],['Formas de pagamento','/payment-methods'],['Condições de pagamento','/payment-terms'],['Bancos','/banks'],['Contas bancárias','/bank-accounts']] as const;
export function AppShell():ReactElement{const auth=useAuth();const {session}=auth;return <div className="min-h-screen bg-slate-50 text-slate-950 lg:grid lg:grid-cols-[260px_1fr]"><aside className="border-b border-slate-200 bg-slate-950 text-white lg:min-h-screen lg:border-b-0"><div className="flex h-16 items-center gap-3 px-5 text-lg font-bold"><span className="grid size-9 place-items-center rounded-xl bg-teal-600">FC</span>FinControl</div><nav className="flex gap-1 overflow-x-auto px-3 pb-3 lg:grid lg:pb-0" aria-label="Menu principal">{links.map(([label,to])=><NavLink key={to} to={to} end={to==='/'} className={({isActive})=>`whitespace-nowrap rounded-lg px-3 py-2.5 text-sm font-medium transition ${isActive?'bg-teal-600 text-white':'text-slate-300 hover:bg-slate-800 hover:text-white'}`}>{label}</NavLink>)}</nav></aside><div><header className="flex min-h-16 items-center justify-between border-b border-slate-200 bg-white px-4 sm:px-6"><div><p className="text-sm font-semibold">{session?.user.fullName}</p><p className="text-xs text-slate-500">{session?.user.roles.join(', ')||'Usuário'}</p></div><button className="rounded-lg px-3 py-2 text-sm font-semibold text-slate-600 hover:bg-slate-100" onClick={()=>void auth.signOut()}>Sair</button></header><main className="p-4 sm:p-6 lg:p-8"><Outlet/></main></div></div>;}
