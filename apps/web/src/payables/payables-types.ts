export interface PayableListItem {id:string;documentNumber:string;documentSeries?:string|null;description:string;supplierName:string;categoryName:string;statusCode:string;totalAmount:string|number;openBalance:string|number;issueDate:string;firstDueDate?:string|null;paymentMethodName?:string|null}
export interface Installment {id?:string;installmentNumber:number;installmentCount:number;amount:number;dueDate:string;paymentMethodId:string;statusCode?:string;notes?:string|null}
export interface PayableDetail extends PayableListItem {supplierId:string;categoryId:string;documentTypeId:string;paymentTermId?:string|null;costCenterId?:string|null;originalAmount:string|number;discountAmount:string|number;additionalAmount:string|number;notes?:string|null;installments:Installment[];approvals:Record<string,unknown>[];attachments:Record<string,unknown>[];tags:Record<string,unknown>[];payments:Record<string,unknown>[]}
export interface ListResponse<T>{data:T[];page:number;pageSize:number;total:number}
export function currency(value:string|number):string{return new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(value));}
export function statusLabel(code:string):string{return {DRAFT:'Rascunho',OPEN:'Em aberto',IN_APPROVAL:'Em aprovação',APPROVED:'Aprovado',SCHEDULED:'Programado',PARTIALLY_PAID:'Parcialmente pago',PAID:'Pago',OVERDUE:'Atrasado',REJECTED:'Reprovado',CANCELLED:'Cancelado'}[code]??code;}
